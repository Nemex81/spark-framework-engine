[tool.pytest.ini_options]
testpaths = ["tests"]

[project]
name = "spark-framework-engine"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
    "pytest>=9.0",
    "pytest-asyncio>=0.23",
    "pytest-cov",
]
