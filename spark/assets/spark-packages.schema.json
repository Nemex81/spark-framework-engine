{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SPARK Packages Declaration",
  "description": "Dichiara i pacchetti SCF da installare automaticamente al primo avvio del workspace.",
  "type": "object",
  "properties": {
    "packages": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Lista ID pacchetti da installare al primo avvio",
      "examples": [["spark-base", "scf-master-codecrafter"]]
    },
    "auto_install": {
      "type": "boolean",
      "default": true,
      "description": "Se false, disabilita l'installazione automatica dei pacchetti elencati"
    }
  },
  "required": ["packages"]
}
