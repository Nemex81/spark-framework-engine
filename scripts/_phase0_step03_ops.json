[
  {
    "start": "_MANIFEST_SCHEMA_VERSION: str = \"3.0\"\n_SUPPORTED_MANIFEST_SCHEMA_VERSIONS: frozenset[str] = frozenset(\n    {\"1.0\", \"2.0\", \"2.1\", \"3.0\"}\n)\n_LEGACY_MANIFEST_SCHEMA_VERSIONS: frozenset[str] = frozenset({\"1.0\", \"2.0\", \"2.1\"})\n# Constants _MANIFEST_FILENAME and _BOOTSTRAP_PACKAGE_ID moved to\n# spark.core.constants (re-exported in header).",
    "end": "\n\n\ndef _resolve_package_version(",
    "replacement": "# Manifest schema constants moved to spark.core.constants (re-exported in header)."
  },
  {
    "start": "class ManifestManager:\n    \"\"\"Read, write and query the SCF installation manifest",
    "end": "\n\n\nclass SnapshotManager:",
    "replacement": "# ManifestManager moved to spark.manifest.manifest (re-exported in header)."
  },
  {
    "start": "class SnapshotManager:\n    \"\"\"Manage UTF-8 BASE snapshots stored under .github/runtime/snapshots/.\"\"\"",
    "end": "\n\n\ndef _normalize_remote_file_record(",
    "replacement": "# SnapshotManager moved to spark.manifest.snapshots (re-exported in header)."
  },
  {
    "start": "def _normalize_remote_file_record(\n    package_id: str,",
    "end": "\n\n\n# MergeSessionManager moved to spark.merge.sessions",
    "replacement": "# _normalize_remote_file_record / _scf_diff_workspace / _scf_backup_workspace\n# moved to spark.manifest (re-exported in header)."
  },
  {
    "start": "_REGISTRY_URL: str = (\n    \"https://raw.githubusercontent.com/Nemex81/scf-registry/main/registry.json\"\n)\n_REGISTRY_CACHE_FILENAME: str = \".scf-registry-cache.json\"\n_REGISTRY_TIMEOUT_SECONDS: int = 5",
    "end": "\n\n\nclass RegistryClient:",
    "replacement": "# Registry endpoint constants moved to spark.core.constants (re-exported in header)."
  }
]
