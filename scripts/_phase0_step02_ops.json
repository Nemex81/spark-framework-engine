[
  {
    "start": "# MergeEngine moved to spark.merge.engine (re-exported in header).\n        \"\"\"Merge three text versions using explicit clean-path rules first.\"\"\"",
    "end": "\n\n\n# Utility helpers _utc_now",
    "replacement": "# MergeEngine moved to spark.merge.engine (re-exported in header).\n\n\n# Utility helpers _utc_now"
  },
  {
    "start": "_SUPPORTED_CONFLICT_MODES: set[str] = {\"abort\", \"replace\", \"manual\", \"auto\", \"assisted\"}",
    "end": "\n\n\ndef _section_markers_for_package(package_id: str) -> tuple[str, str]:",
    "replacement": "# Validators / merge-text helpers moved to spark.merge.validators\n# (re-exported in header)."
  },
  {
    "start": "def _section_markers_for_package(package_id: str) -> tuple[str, str]:",
    "end": "\n\n\n# ---------------------------------------------------------------------------\n# FrameworkInventory\n# ---------------------------------------------------------------------------",
    "replacement": "# SCF section helpers moved to spark.merge.sections (re-exported in header)."
  },
  {
    "start": "class MergeSessionManager:\n    \"\"\"Manage manual merge sessions persisted under .github/runtime/merge-sessions/.\"\"\"",
    "end": "\n\n\n# ---------------------------------------------------------------------------\n# RegistryClient (A4",
    "replacement": "# MergeSessionManager moved to spark.merge.sessions (re-exported in header)."
  }
]
